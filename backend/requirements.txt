fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.20.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
# TikTokLive 7.x pulls betterproto2[pydantic], which requires Pydantic 2.11+.
pydantic==2.11.5
pydantic-settings==2.5.2
redis==5.0.8
python-multipart==0.0.9
websockets==13.1
aiofiles==24.1.0
# TikTokLive 7.x requires httpx >= 0.28.1.
httpx==0.28.1
Pillow==10.4.0
# Used to cut a flat background out of generated art. It already arrives as
# a transitive dependency, but the cutout depends on it directly.
# The production TikTok LIVE event client. Keep this pinned: TikTok's
# Webcast protocol evolves and a deliberate upgrade/test is safer than an
# unexpected image rebuild changing the event schema.
TikTokLive==7.0.1
